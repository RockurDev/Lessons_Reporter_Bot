meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12.5
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /data
  containerPort: 80
  command: lessons_reporter_bot/main.py
